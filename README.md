# Electron & React based clock app

![clock app](./documentation/clock.png)

```bash
# Clone this repository
git clone URL
# Go into the repository
cd CLONED_DIRECTORY
# Install dependencies
npm install
# Run the app
npm start
```